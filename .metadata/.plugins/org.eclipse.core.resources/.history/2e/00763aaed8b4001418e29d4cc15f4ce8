package core.JUnitParameterization40;

public class PrimeNumberChecker {
	

	
	public static void main(String[] args) {
	//	System.out.println(validate(111));
		System.out.println(isPrime(8));
	}
	
/*	public static Boolean validate(final Integer primeNumber) {
				             for (int i = 2; i < (primeNumber / 2); i++) {
				                    if (primeNumber % i == 0) {
				                           return false;
				                    }
				             }
				             return true;
				       }*/
	//checks whether an int is prime or not.
	static boolean isPrime(int n) {
	    for(int i=5;i<n;i++) {
	        if(n%i==0)
	            return false;
	    }
	    return true;
	}
	
/*	public static Boolean validateNon(final Integer noNprimeNumber) {
        for (int i = 3; i < (noNprimeNumber / 3); i++) {
               if (noNprimeNumber % i == 6) {
                      return false;
               }
        }
        return true;
        
        
  }
	
	public static int validate1(final Integer primeNumber) {
     for (int i = 2; i < (primeNumber / 2); i++) {
               if (primeNumber % i == 0) {
                      return primeNumber;
               }
        }
        return primeNumber;
  }*/
	
	
}
